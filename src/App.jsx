/**
 * Soft Atelier — Verdant Commerce App entry
 * Single dashboard route rendered inside the theme container.
 */
import Dashboard from "@/components/dashboard/Dashboard";

export default function App() {
  return <Dashboard />;
}
